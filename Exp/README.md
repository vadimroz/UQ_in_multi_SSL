# Experiment Weights

This folder contains the weights for the SRP-DNN model. These weights are used for sound localization experiments and 
are essential for reproducing the results of the SRP-DNN-based methods.

Ensure that the weights are correctly loaded during the execution of the relevant scripts.
