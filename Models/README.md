# Model artifacts

The pretrained artifacts are organized by model family:

- [`SRP-DNN/`](SRP-DNN/README.md): SRP-DNN checkpoint, provenance, and citation.
- [`MDN/`](MDN/README.md): adapted mixture-density network checkpoints and
  provenance.

The two model families have different origins and should not be treated as the
same network.
